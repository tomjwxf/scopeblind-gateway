{
  "name": "scopeblind-gateway",
  "version": "1.0.0",
  "private": true,
  "description": "ScopeBlind Gateway — Cloudflare Worker reverse proxy with shadow mode",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail --format json | jq 'select(.logs[]?.message | contains(\"_scopeblind\"))'"
  },
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240923.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.80.0"
  }
}
